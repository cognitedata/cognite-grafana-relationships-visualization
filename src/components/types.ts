import { DefaultOptions } from '../types';
export { AVOIDED_KEY } from '../types';
export interface PanelEditorProps {
  onChange: any;
  settings: DefaultOptions;
}
export interface Direction {
  [x: string]: string;
}
